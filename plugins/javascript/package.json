{
  "name": "@omniflow/javascript-plugin",
  "version": "1.0.0",
  "description": "Official JavaScript plugin template for OmniFlow — a modular workflow automation engine.",
  "author": "TheSkiF4er",
  "license": "Apache-2.0",
  "type": "module",

  "main": "dist/index.js",
  "types": "dist/index.d.ts",

  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },

  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "rimraf dist && tsc && rollup -c",
    "test": "jest --runInBand",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "format": "prettier --write .",
    "prepare": "npm run build"
  },

  "keywords": [
    "omniflow",
    "workflow",
    "automation",
    "plugin",
    "javascript",
    "modular",
    "extensions"
  ],

  "engines": {
    "node": ">=18"
  },

  "dependencies": {
    "@omniflow/sdk": "^1.0.0"
  },

  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.0",
    "rimraf": "^6.0.0",
    "rollup": "^4.12.0",
    "rollup-plugin-terser": "^7.0.2",
    "rollup-plugin-typescript2": "^0.36.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.6.0"
  }
}
